---
apiVersion: v1
kind: Service
metadata:
  name: llm-gateway
  labels:
    app: llm-gateway
spec:
  type: LoadBalancer
  selector:
    app: llm-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
---